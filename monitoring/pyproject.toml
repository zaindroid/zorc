[project]
name = "zorc-watchdog"
version = "0.1.0"
description = "Host watchdog + status page + node self-registration for servingz"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "httpx>=0.28.1",
    "pyyaml>=6.0",
    "uvicorn[standard]>=0.52.1",
]
